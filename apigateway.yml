server:
  port: 8072
eureka:
  instance:
    preferIpAddress: true #tell eureka to use IP address for accounts
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/
